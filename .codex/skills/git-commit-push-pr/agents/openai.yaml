interface:
  display_name: "Git Commit Push PR"
  short_description: "Commit, review, push, and open a PR"
  default_prompt: "Use $git-commit-push-pr to commit my changes, push the branch, run focused review agents, and create or update the GitHub PR."
