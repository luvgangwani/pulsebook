interface:
  display_name: "Git Commit Push PR"
  short_description: "Commit, push, and open a PR"
  default_prompt: "Use $git-commit-push-pr to commit my changes, push the branch, and create or update the GitHub PR."
